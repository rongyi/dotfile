[alias]
  ls = status -sb
  cm = commit
  lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
  co = checkout
[core]
  ediotr = vim
  autocrlf = input
  fileMode = false
  editor = vim -c 'startinsert'
  pager = delta --theme OneHalfLight
[user]
  name = rongyi
  email = rongyi@onchain.com
[color]
  ui = true
[branch]
  autosetuprebase = always
[pull]
  rebase = true
[push]
  default = upstream
[diff]
  guitool = meld
  tool = meld
[difftool]
  prompt = false
[credential]
  helper = cache --timeout=54000
[interactive]
  diffFilter = delta --theme OneHalfLight
